[project]
name = "presenton"
version = "1.0.0"
description = "Open-source AI presentation generator — DDD + MCP architecture"
requires-python = ">=3.11,<3.13"

[tool.uv.workspace]
members = ["shared", "domains/*", "gateway"]

[tool.pytest.ini_options]
pythonpath = [".", "shared"]
