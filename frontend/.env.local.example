# API Gateway URL (used for server-side rewrites in next.config.mjs)
GATEWAY_URL=http://localhost:8000

# Public API URL (used by the browser/client-side code)
NEXT_PUBLIC_API_URL=/api/v1

# WebSocket URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000/api/v1
